.home-page {
  min-height: 100vh;
  background: #f8fafc;
}

/* Header */
.home-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 32rpx 24rpx;
  padding-top: calc(24rpx + env(safe-area-inset-top));
  background: linear-gradient(135deg, #fff5f7 0%, #fff9f0 100%);
  border-bottom: 1rpx solid rgba(244,63,94,0.08);
}

/* 未登录时顶部登录按钮 */
.login-btn-hd {
  background: linear-gradient(135deg, #f43f5e, #fb923c);
  border-radius: 32rpx;
  padding: 12rpx 32rpx;
  flex-shrink: 0;
}
.login-btn-hd:active { opacity: 0.8; }
.login-btn-hd-text {
  font-size: 26rpx;
  font-weight: 700;
  color: #fff;
}

/* 游客引导横幅 */
.guest-banner {
  margin: 20rpx 32rpx 0;
  background: linear-gradient(135deg, #fff1f2, #fff7ed);
  border: 1rpx solid rgba(244,63,94,0.15);
  border-radius: 20rpx;
  padding: 28rpx 32rpx;
  display: flex;
  align-items: center;
  gap: 24rpx;
}
.guest-banner:active { opacity: 0.8; }
.guest-banner-icon { font-size: 48rpx; flex-shrink: 0; }
.guest-banner-text {
  display: flex;
  flex-direction: column;
  gap: 6rpx;
  flex: 1;
}
.guest-banner-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #1e293b;
}
.guest-banner-sub {
  font-size: 24rpx;
  color: #f43f5e;
}

.header-left {
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex: 1;
  min-width: 0;
}
.header-left:active { opacity: 0.8; }

/* 头部头像容器 */
.avatar-wrap-hd {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  flex-shrink: 0;
  box-shadow: 0 0 0 4rpx rgba(244,63,94,0.18);
  overflow: hidden;
}

.avatar-img-hd {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  display: block;
}

.avatar-fallback-hd {
  width: 88rpx;
  height: 88rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #f43f5e, #fb923c);
  display: flex;
  align-items: center;
  justify-content: center;
}

.avatar-fallback-hd text {
  color: #fff;
  font-size: 34rpx;
  font-weight: 700;
}

.avatar-logo-emoji {
  font-size: 40rpx;
  line-height: 1;
}

.header-text { flex: 1; min-width: 0; }

.welcome-text {
  font-size: 30rpx;
  font-weight: 700;
  color: #1e293b;
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.welcome-sub {
  font-size: 22rpx;
  color: #f43f5e;
  display: block;
  margin-top: 6rpx;
  opacity: 0.7;
}

.icon-btn {
  width: 68rpx;
  height: 68rpx;
  border-radius: 50%;
  background: rgba(255,255,255,0.8);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.06);
  flex-shrink: 0;
}
.icon-btn:active { background: #fce7eb; }

/* Search */
.search-wrap {
  padding: 20rpx 32rpx;
  background: #fff;
  border-bottom: 1rpx solid rgba(0,0,0,0.05);
}

.search-bar {
  display: flex;
  align-items: center;
  background: #f8fafc;
  border: 1rpx solid #e2e8f0;
  border-radius: 20rpx;
  padding: 0 24rpx;
  height: 76rpx;
  gap: 12rpx;
}

.search-icon { font-size: 28rpx; color: #94a3b8; }

.search-input {
  flex: 1;
  font-size: 28rpx;
  color: #1e293b;
  background: transparent;
}

.search-clear {
  color: #94a3b8;
  font-size: 28rpx;
  padding: 8rpx;
}

/* Scroll */
.home-scroll {
  height: calc(100vh - 200rpx);
}

/* Content section */
.content-section {
  padding: 28rpx 32rpx 0;
}

/* Contact card */
.contact-card {
  display: flex;
  align-items: center;
  gap: 24rpx;
  background: #fff;
  border-radius: 24rpx;
  border: 1rpx solid rgba(0,0,0,0.06);
  padding: 24rpx;
  margin-bottom: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}
.contact-card:active { opacity: 0.8; }

.contact-avatar-wrap {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #f43f5e, #fb923c);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
  overflow: hidden;
}

.avatar-img { width: 80rpx; height: 80rpx; border-radius: 50%; display: block; }

.avatar-letter {
  color: #fff;
  font-size: 28rpx;
  font-weight: 700;
}

.contact-info { flex: 1; min-width: 0; }

.contact-name-row {
  display: flex;
  align-items: center;
  gap: 8rpx;
  overflow: hidden;
}

.contact-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #1e293b;
  flex-shrink: 1;
  min-width: 0;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}


.zodiac-tag {
  flex-shrink: 1;
  min-width: 0;
  font-size: 20rpx;
  font-weight: 500;
  color: #7c3aed;
  background: #f5f3ff;
  padding: 2rpx 10rpx;
  border-radius: 6rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.contact-sub {
  font-size: 22rpx;
  color: #94a3b8;
  display: block;
  margin-top: 4rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.days-badge {
  flex-shrink: 0;
  padding: 6rpx 16rpx;
  border-radius: 100rpx;
  font-size: 22rpx;
  font-weight: 600;
}
.days-badge.imminent { background: #fff1f2; color: #f43f5e; }
.days-badge.soon { background: #fff7ed; color: #f97316; }
.days-badge.monthly { background: #f1f5f9; color: #64748b; }

.arrow { color: #cbd5e1; font-size: 36rpx; }

/* Event card */
.event-card {
  display: flex;
  align-items: center;
  gap: 20rpx;
  background: #fff;
  border-radius: 24rpx;
  border: 1rpx solid rgba(0,0,0,0.06);
  padding: 24rpx;
  margin-bottom: 16rpx;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.04);
}
.event-card:active { opacity: 0.8; }

.event-icon-wrap {
  width: 80rpx;
  height: 80rpx;
  border-radius: 20rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  flex-shrink: 0;
}
.event-icon-wrap.rose { background: #fff1f2; }
.event-icon-wrap.orange { background: #fff7ed; }
.event-icon-wrap.violet { background: #f5f3ff; }

.event-info { flex: 1; min-width: 0; }

.event-name {
  font-size: 28rpx;
  font-weight: 600;
  color: #1e293b;
  display: block;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.event-sub {
  font-size: 22rpx;
  color: #94a3b8;
  display: block;
  margin-top: 4rpx;
}

.event-meta { flex-shrink: 0; text-align: right; }

.event-days {
  display: block;
  font-size: 24rpx;
  font-weight: 600;
}
.event-days.rose { color: #f43f5e; }
.event-days.orange { color: #f97316; }
.event-days.muted { color: #94a3b8; }

.event-year { font-size: 20rpx; color: #94a3b8; display: block; }

.event-badge {
  flex-shrink: 0;
  padding: 4rpx 14rpx;
  border-radius: 100rpx;
  font-size: 20rpx;
  font-weight: 500;
}

/* Search sub-title */
.search-sub-title {
  font-size: 24rpx;
  color: #94a3b8;
  font-weight: 500;
  padding: 0 4rpx;
  margin-bottom: 16rpx;
  display: flex;
  align-items: center;
  gap: 8rpx;
}
.search-sub-title::before {
  content: '';
  display: block;
  width: 4rpx;
  height: 24rpx;
  background: currentColor;
  border-radius: 2rpx;
  opacity: 0.4;
}

.empty-tip {
  text-align: center;
  color: #94a3b8;
  font-size: 26rpx;
  padding: 80rpx 0;
}

/* Empty state */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 120rpx 48rpx;
  text-align: center;
}

.empty-icon-wrap {
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  background: #fff1f2;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 32rpx;
}

.empty-title { font-size: 32rpx; font-weight: 700; color: #1e293b; margin-bottom: 12rpx; display: block; }
.empty-desc { font-size: 26rpx; color: #94a3b8; display: block; }

/* FAB */
.fab-mask {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.3);
  z-index: 40;
}

.fab-container {
  position: fixed;
  right: 48rpx;
  bottom: calc(40rpx + env(safe-area-inset-bottom));
  z-index: 50;
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 16rpx;
}

.fab-menu {
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-bottom: 8rpx;
}

.fab-item {
  display: flex;
  align-items: center;
  gap: 16rpx;
  justify-content: flex-end;
}
.fab-item:active { opacity: 0.8; }

.fab-item-icon {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 36rpx;
  box-shadow: 0 4rpx 16rpx rgba(0,0,0,0.12);
}
.rose-bg { background: #fff1f2; }
.orange-bg { background: #fff7ed; }
.violet-bg { background: #f5f3ff; }

.fab-item-label {
  font-size: 24rpx;
  font-weight: 500;
  background: rgba(255,255,255,0.95);
  padding: 8rpx 20rpx;
  border-radius: 100rpx;
  color: #1e293b;
  box-shadow: 0 2rpx 12rpx rgba(0,0,0,0.1);
}

.fab-btn {
  width: 104rpx;
  height: 104rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #f43f5e, #fb7185);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 8rpx 24rpx rgba(244,63,94,0.35);
  transition: transform 0.2s;
}
.fab-btn.fab-open { background: #64748b; }
.fab-btn:active { transform: scale(0.92); }

.fab-icon {
  color: #fff;
  font-size: 52rpx;
  font-weight: 300;
  line-height: 1;
}

/* Settings */
.settings-sheet { max-height: 60vh; }

.settings-section-title {
  font-size: 22rpx;
  color: #94a3b8;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 1rpx;
  margin-bottom: 12rpx;
  display: block;
}

/* Modal (reused from global) */
.modal-mask {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.4);
  z-index: 100;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
}

.modal-sheet {
  background: #fff;
  border-radius: 32rpx 32rpx 0 0;
  display: flex;
  flex-direction: column;
  padding-bottom: env(safe-area-inset-bottom);
}

.modal-handle {
  width: 80rpx;
  height: 8rpx;
  background: #e2e8f0;
  border-radius: 4rpx;
  margin: 16rpx auto 0;
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 40rpx;
  border-bottom: 1rpx solid #f1f5f9;
}

.modal-title { font-size: 32rpx; font-weight: 600; }

.modal-close {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  background: #f1f5f9;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 24rpx;
  color: #64748b;
}

.section-header {
  display: flex;
  align-items: center;
  gap: 12rpx;
  margin-bottom: 20rpx;
}

.section-bar {
  width: 6rpx;
  height: 32rpx;
  border-radius: 3rpx;
}

.section-title { font-size: 26rpx; font-weight: 700; }

.section-count {
  font-size: 22rpx;
  padding: 2rpx 14rpx;
  border-radius: 100rpx;
}

.loading-wrap {
  text-align: center;
  color: #94a3b8;
  font-size: 26rpx;
  padding: 80rpx 0;
}

.toggle-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 0;
  border-bottom: 1rpx solid #f1f5f9;
}

.toggle-info { flex: 1; margin-right: 24rpx; }
.toggle-label { font-size: 28rpx; font-weight: 500; display: block; }
.toggle-desc { font-size: 22rpx; color: #94a3b8; display: block; margin-top: 4rpx; }

.settings-nav-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24rpx 0;
  border-top: 1rpx solid #f1f5f9;
}
.nav-arrow {
  font-size: 44rpx;
  color: #cbd5e1;
  font-weight: 300;
  line-height: 1;
}

/* ── 设置面板新样式 ────────────────────────────────────────────────────────── */
.settings-body {
  flex: 1;
  max-height: 72vh;
}

/* 设置面板内边距 */
.settings-inner {
  padding: 0 36rpx;
}

/* 头像区域 */
.profile-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 40rpx 0 24rpx;
}

/* 头像点击区域（普通 view 替代 open-type button） */
.avatar-tap-wrap {
  position: relative;
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  overflow: hidden;
  box-shadow: 0 4rpx 20rpx rgba(0,0,0,0.14);
}
.profile-avatar-img {
  width: 160rpx;
  height: 160rpx;
  border-radius: 50%;
  display: block;
}

/* 上传中遮罩 */
.avatar-uploading-mask {
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
}
.avatar-uploading-text {
  font-size: 20rpx;
  color: #fff;
}

/* 昵称展示行（头像下方，按钮上方） */
.nickname-info-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  margin-top: 28rpx;
  padding: 0 8rpx;
}
.nickname-info-label {
  font-size: 28rpx;
  color: #64748b;
}
.nickname-info-value {
  font-size: 28rpx;
  color: #1e293b;
  font-weight: 500;
  max-width: 60%;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 微信头像/昵称一键获取按钮 */
.wx-profile-btn {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  background: #f97316;
  color: #fff;
  font-size: 30rpx;
  font-weight: 600;
  border-radius: 50rpx;
  border: none;
  padding: 0;
  margin-top: 28rpx;
}
.wx-profile-btn::after { border: none; }
.wx-profile-btn:active { opacity: 0.85; }

/* 设置分组 */
.settings-item-group {
  margin-bottom: 32rpx;
}

.settings-group-label {
  font-size: 22rpx;
  color: #94a3b8;
  font-weight: 500;
  letter-spacing: 1rpx;
  display: block;
  margin-bottom: 12rpx;
}

.settings-hint {
  font-size: 20rpx;
  color: #94a3b8;
  display: block;
  margin-top: 8rpx;
}

/* 退出按钮 */
.logout-btn {
  width: 100%;
  height: 88rpx;
  border-radius: 20rpx;
  background: #fff;
  color: #f43f5e;
  font-size: 28rpx;
  font-weight: 500;
  border: 1rpx solid #fecdd3;
  display: flex;
  align-items: center;
  justify-content: center;
}
.logout-btn::after { border: none; }
.logout-btn:active { opacity: 0.75; }

/* ── 微信资料授权弹窗 ──────────────────────────────────────────────────────── */
.wx-auth-sheet {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: #fff;
  border-radius: 32rpx 32rpx 0 0;
  padding: 0 48rpx 60rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.wx-auth-title {
  font-size: 34rpx;
  font-weight: 700;
  color: #1e293b;
  margin-top: 40rpx;
  display: block;
  text-align: center;
}

.wx-auth-sub {
  font-size: 24rpx;
  color: #94a3b8;
  margin-top: 10rpx;
  display: block;
  text-align: center;
}

/* 步骤提示文字（button 外部，不在 button 内） */
.wx-auth-step-label {
  font-size: 24rpx;
  color: #94a3b8;
  display: block;
  width: 100%;
  margin-top: 32rpx;
  margin-bottom: 8rpx;
}

/* 头像选择按钮：open-type="chooseAvatar"，内部只放 image */
.wx-auth-avatar-btn {
  width: 180rpx;
  height: 180rpx;
  border-radius: 50%;
  overflow: hidden;
  background: #e2e8f0;
  border: none;
  padding: 0;
  margin-top: 8rpx;
}
.wx-auth-avatar-btn::after { border: none; }
.wx-auth-avatar-btn:active { opacity: 0.8; }

/* image 必须在 button 内部，始终存在，src 为默认图或真实头像 */
.wx-auth-avatar-img {
  width: 180rpx;
  height: 180rpx;
  border-radius: 50%;
  display: block;
}

/* 昵称输入（type="nickname" 触发微信昵称建议） */
.wx-auth-nickname-input {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  background: #f8fafc;
  border: 2rpx solid #e2e8f0;
  border-radius: 20rpx;
  padding: 0 32rpx;
  font-size: 30rpx;
  color: #1e293b;
  margin-top: 36rpx;
  box-sizing: border-box;
}
.wx-auth-nickname-ph { color: #cbd5e1; }

/* 完成按钮 */
.wx-auth-confirm-btn {
  width: 100%;
  height: 92rpx;
  line-height: 92rpx;
  background: #f97316;
  color: #fff;
  font-size: 32rpx;
  font-weight: 700;
  border-radius: 50rpx;
  border: none;
  padding: 0;
  margin-top: 32rpx;
}
.wx-auth-confirm-btn::after { border: none; }
.wx-auth-confirm-btn:active { opacity: 0.85; }

/* 跳过按钮 */
.wx-auth-skip-btn {
  width: 100%;
  height: 80rpx;
  line-height: 80rpx;
  background: transparent;
  color: #94a3b8;
  font-size: 28rpx;
  border: none;
  padding: 0;
  margin-top: 16rpx;
}
.wx-auth-skip-btn::after { border: none; }
.wx-auth-skip-btn:active { opacity: 0.7; }
